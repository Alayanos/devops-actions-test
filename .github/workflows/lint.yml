name: Use Central Lint

on:
  push:
    branches: [main]
  pull_request:
  
jobs:
  lint:
    uses: alayanos/devops-actions-poc/.github/workflows/lint-golangci.yml@main
    with:
      go-version: '1.21'
